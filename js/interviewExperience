swiggy :
rounds

- coding :- 2 hours - api ,fetching data and the searching , sorting
  filtering, searching , debouncing,type ahead feature, responsive
  ,structure, web performance

- technical interview -aprox 1 hour
  DS+ UI - DFS and BFS / tree searching kind of
  BFS: breath first search-
  10
  4 6
  11 13 14 15

BFS : 10 4 6 11 13 14 15
DFS : 10 4 11 13 6 14 15
prototypal inheritence
promises
aync await
web performance optimization

- UI design and arcitur -aprox 1 hour
  to implement Carousel - own lib for that
  responsive

- snake game
- ## Hiring maneger -aprox 1 hour

#Unacademy

1. diff b/w map and forEach

   - map returns new array but forEach does not return anaything
   - map didnot modifiy original array but forEach modified original array
   - we can chain stuff on map

2. null vs undefine
   null is an actual value but undefine indicate that veriable is declear but not intilize

3. undefine vs not define
   if we declare varible but not intilize it the value will be undefine
   if we didnot declare variable itself the we will get not defined

4. event delegation
   generally when we work on any website , inside of attacting event listener to all
   product or item we can attact event lister to parent and handle ir accordingly

5. Flatten the Array
6. call apply and bind
   object.method.call(objectonwhichwewanttocall.value) / /it directly run taht function
   object.method.apply(objectonwhichwewanttocall.[value])
   object.method.bind(objectonwhichwewanttocall) // it will reeturn a function

7. infinite currying

function add(a){
return function(b){
if(b) return add(a+b)
return a;
}
}

console.log(add(5)(2)())

8. composition polyfill

9. debouncing

